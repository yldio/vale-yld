The tests pass on Linux. Moreover, they pass on macOS.
The build is fast. Furthermore, it is deterministic.
The API is stable. Additionally, it is well-documented.
The new approach is faster. On the other hand, it uses more memory.
The tool supports JSON. In addition, it supports YAML.
